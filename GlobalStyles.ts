import { StyleSheet, TextInput, View, Text } from 'react-native'

export const GlobalStyles = StyleSheet.create({
    border: {
        borderStyle: 'solid',
        borderWidth: 1,
        borderColor: 'black',
    },
})
